# 功能点决策记录（用户 2026-09 定）
原则: 功能点不留在聊天里, 落档管理; 新想法先入表再排期。

## 已定
| 功能点 | 决策 | 理由 |
|---|---|---|
| 锁定连续听写(点按锁定/再点结束) | **做** | 用户认可, 长听写场景 |
| 偏差审计(逐功能对照 Handy 实现方式) | **做(进行中)** | 用户: "当前功能都按人家已实现方式去看, 有没有没按人家方式实现可能导致问题的" |

## 待定(用户提及"可能做")
| 功能点 | 触发条件 |
|---|---|
| 离线引擎(sherpa-onnx SenseVoice, 研究课题 E4 已选型) | 用户需要离线/隐私场景 |
| 自动更新(Tauri updater) | 正式分发前 |
| 流式直写(边说边把 partial 写进光标, 打字机) | 用户提及"抓取"可能指此, 待确认 |

## 不做(评估后排除)
| 功能点 | 排除理由 |
|---|---|
| 菜单栏点图标直接录音 | 与锁定听写/热键场景重叠 |
| LLM 差异自动建议词条 | 手动"加为词条"已覆盖, 自动建议误加率高 |
| 历史导出 | jsonl 已在数据目录, 需要时再说 |
| 浮窗拖拽+位置记忆 | 浮窗跟随光标定位已定版, 加拖拽引入状态复杂度 |
